http:
  routers:
    pihole:
      rule: "Host(`pihole.richardsoper.me`)"
      service: pihole-service
      entryPoints:
        - websecure
      tls:
        certResolver: cf-cert

  services:
    pihole-service:
      loadBalancer:
        servers:
          - url: "http://192.168.1.14:80"
